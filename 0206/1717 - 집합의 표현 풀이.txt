이 문제는 기본적인 유니온 파인드 문제이다
initi() 으로 초기화를 시키고
각각 명령에 따라 merge, find를 통해서 값을 출력,실행 시킨다

Rank를 통해 각 층을 알아놓고 활용했는데
1) rank가 같으면 merge시 층이 하나 늘어나기 때문에 ++ 시켜주고

2) 높은 층 루트에 낮은 층 루트를 달아주면 층이 늘어나지 않는다

그러한 생각을 통해서 풀이한다
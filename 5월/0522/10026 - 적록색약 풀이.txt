board에 값을 입력받고 같은 색깔이 인접해있으면 체크해주는 식으로 DFS를 돌렸다.
전체 board를 DFS를 돌리고 DFS한번 돌릴때마다 ret++를 해주고 출력을 해줬다.

두 번의 경우를 체크해줬는데
적록색약의 경우 R,G가 같은 색으로 보이기 때문에
board2를 따로 만들어서 체크를 해줬다.

즉 2개의 board를 만들고 각각에 대해 DFS를 통한 체크를 통해서 풀었다.
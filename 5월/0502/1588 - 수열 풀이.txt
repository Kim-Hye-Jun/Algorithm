이 문제는 분할정복의 느낌을 느끼기 좋은 문제라고 생각한다.

1))) 일단 각각 몇초가 지났을 때 1,2,3의 개수를 구할 수 있다.
	이걸 저장을 해놓고

2)))) Left , Right 값에 따라서
start ~ end값이 L~R 안에 완전히 포함되어 있으면 결과값에 각 숫자의 개수를 + 시켜준다.
			걸쳐 있다면 세 부위로 나눠서 (0 ~1/3 , 1/3~2/3, 2/3~1 ) 각각 재귀함수를 호출한다.
			아예 포함되어있지 않다면 볼 필요 없으니 return


실버문제라 그런지 쉽게 풀었다.
그런데 신기한 풀이? 를 알게 되었다.

for(scanf("%d",&a);scanf("%c",&c),c!=10;)
  {
    if(c=='-') k=1;
    scanf("%d",&b);
    if(k==0)
      a+=b;
    else
      a-=b;
  }

이런식으로 문자열 파싱을 할 수 있다는 걸 공부할 수  있었다!
한 문자열을 입력을 받는데 처음에 숫자를 받고 연산자가 있을때 마다 입력을 받는 식이다.
이 부분을 숫자, 연산자 따로 리스트를 만들어 처리하면 더 좋을 것 같은데
19591 독특한 계산기 문제를 그런 방식으로 풀었다.
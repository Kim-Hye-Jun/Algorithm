이 문제는 서로 다른 세 수의 합의 절댓값의 최솟값을 구하는 문제이다.

크기비교를 쉽게 하기 위해 입력값들을 일단 정렬한다.
정렬 후에 세 값을 선택 후 abs의 최솟값을 찾는데
세 값을 선택할 때에는 맨 앞 , 가운데 , 맨 뒤의 값을 정하는데
맨앞, 맨뒤의 값은 i ~ N-3 ,  i+2 ~ N-1 의 범위에서 고르고
가운데 값을 lower_bound 값으로 선택한다.
그리고 lower_bound값, 바로 그 앞에값을
선택해 더하고 abs의 최솟값을 구한다.

